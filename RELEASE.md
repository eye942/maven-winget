# Release checklist

Run `pwsh ./scripts/New-Release.ps1 -MavenVersion <version> -Scope Machine`, review the generated metadata and artifacts, then dispatch the release workflow. Publication is always a maintainer action.
