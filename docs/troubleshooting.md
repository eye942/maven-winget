# Troubleshooting

Open a new terminal after install. If `mvn` is missing, inspect the relevant user/system PATH and MSI log (`/l*v install.log`). If Maven says Java is missing, install and configure a compatible JDK (`JAVA_HOME` or `java.exe` on PATH). Use the matching scope for upgrades.
